"""Flavour-pairing data-foundation package.

CP1 scope: configuration loading and validation only (``flavor_pairing.config``).
Later checkpoints add ingestion, parsing, normalization, review, and storage —
see ``docs/DATA_FOUNDATION_PLAN.md``.
"""
